# Cloudflare (Named Tunnel) – required for cloudflared managed tunnel
CLOUDFLARE_TUNNEL_TOKEN=eyJhbGciOi...   # your long tunnel token

# Optional: Public-facing keys your router needs (don't put Notion token here)
# OPENAI_API_KEY=sk-...
