# === ROUTER CONFIGURATION ===
ROUTER_PORT=3032
ROUTER_LOG_LEVEL=info
ROUTER_API_KEY=router-dev-123

# === MCP BACKEND CONFIGURATION ===
# Production MCP server (stable, tested)
MCP_BASE_URL=http://notion_mcp:3030
MCP_AUTH=my-local-mcp-secret

# Development MCP server (clean, minimal)
MCP_DEV_URL=http://notion_mcp_dev:3031

# === RATE LIMITING ===
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=900000

# === METRICS ===
METRICS_REQUIRE_AUTH=true

# === M3: DUAL MCP BACKENDS ===
# Official OpenAI Notion MCP (read-preferred)
OFFICIAL_MCP_URL=http://localhost:9999/mcp    # <-- put the real URL
OFFICIAL_MCP_AUTH=YOUR_OFFICIAL_BEARER_TOKEN

# Your custom MCP (full read/write)
CUSTOM_MCP_URL=http://notion_mcp:3030/mcp
CUSTOM_MCP_AUTH=my-local-mcp-secret

# Timeouts (ms)
MCP_TIMEOUT_MS=12000
